apiVersion: v1
kind: Service
metadata:
  name: my-apache-svc
  labels:
    app: apache2
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    nodePort: 31260
    protocol: TCP
  selector:
    app: apache2
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-apache-svc
  labels:
    app: apache2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: apache2
  template:
    metadata:
      labels:
        app: apache2
    spec:
      containers:
      - name: nginx
        image: manash081/poc3-saheb
        ports:
        - containerPort: 80
